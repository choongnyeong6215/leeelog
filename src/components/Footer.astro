---
import { PROFILE, SITE } from '../config';
const year = new Date().getFullYear();
---

<footer class="border-t border-border py-8">
  <div class="mx-auto max-w-7xl px-4 text-center text-xs text-text-faint">
    © {year} {PROFILE.nickname} · {SITE.title}
  </div>
</footer>
